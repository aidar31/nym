info:
  name: identities
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4434/admin/identities
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
