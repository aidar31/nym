info:
  name: sessions
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4434/admin/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
